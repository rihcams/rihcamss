const fs = require('fs')
const read = (files) => JSON.parse(fs.readFileSync(files))
const err = require('./Errors.js')


function Has(userData) {
    const data = read('./database.json')
    return data[userData] ? true : false
} 




module.exports = Has